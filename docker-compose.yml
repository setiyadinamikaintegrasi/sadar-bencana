name: sadar-bencana

services:
  redis:
    image: redis:7-alpine
    container_name: sadar-redis
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - sadar-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s

  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile.api
    container_name: sadar-api
    restart: unless-stopped
    environment:
      API_HOST: 0.0.0.0
      API_PORT: "8001"
      API_ENV: ${API_ENV:-docker}
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL to the Supabase pooled connection string}
      SUPABASE_URL: ${SUPABASE_URL:?Set SUPABASE_URL}
      SUPABASE_JWT_SECRET: ${SUPABASE_JWT_SECRET:?Set SUPABASE_JWT_SECRET}
      SUPABASE_JWKS_URL: ${SUPABASE_JWKS_URL:-}
      MASTRA_BASE_URL: ${MASTRA_BASE_URL:-http://host.docker.internal:4111}
      MASTRA_API_TOKEN: ${MASTRA_API_TOKEN:?MASTRA_API_TOKEN is required}
      WORKER_BASE_URL: ${WORKER_BASE_URL:-http://worker:8002}
      DEPLOYMENT_MODE: ${DEPLOYMENT_MODE:-community}
      PERSONAL_ASSET_LIMIT: ${PERSONAL_ASSET_LIMIT:-20}
      ENTITLEMENT_PUBLIC_KEY: ${ENTITLEMENT_PUBLIC_KEY:-}
      GEOCODER_BASE_URL: ${GEOCODER_BASE_URL:-https://nominatim.openstreetmap.org}
      GEOCODER_USER_AGENT: ${GEOCODER_USER_AGENT:-SadarBencana/0.1}
      RISK_FREE_LIMIT: ${RISK_FREE_LIMIT:-0}
      AI_BRIEFING_TIMEOUT: ${AI_BRIEFING_TIMEOUT:-150s}
      AI_EXECUTIVE_CACHE_TTL: ${AI_EXECUTIVE_CACHE_TTL:-6h}
      AI_EXECUTIVE_PER_MINUTE: ${AI_EXECUTIVE_PER_MINUTE:-2}
      AI_EXECUTIVE_PER_DAY: ${AI_EXECUTIVE_PER_DAY:-3}
      AI_EXECUTIVE_GLOBAL_PER_DAY: ${AI_EXECUTIVE_GLOBAL_PER_DAY:-20}
      AI_COPILOT_PER_MINUTE: ${AI_COPILOT_PER_MINUTE:-5}
      AI_COPILOT_PER_DAY: ${AI_COPILOT_PER_DAY:-10}
      AI_COPILOT_GLOBAL_PER_MINUTE: ${AI_COPILOT_GLOBAL_PER_MINUTE:-30}
      AI_COPILOT_GLOBAL_PER_DAY: ${AI_COPILOT_GLOBAL_PER_DAY:-100}
      AI_COPILOT_MAX_CHARACTERS: ${AI_COPILOT_MAX_CHARACTERS:-2000}
      OFFICIAL_SOURCE_SETTINGS_KEY: ${OFFICIAL_SOURCE_SETTINGS_KEY:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-noreply@sadarbencana.id}
      CORS_ALLOWED_ORIGINS: ${CORS_ALLOWED_ORIGINS:-http://localhost:3001}
      TRUSTED_PROXIES: ${TRUSTED_PROXIES:-127.0.0.1/32,::1/128,172.16.0.0/12}
      WORKER_API_TOKEN: ${WORKER_API_TOKEN:?WORKER_API_TOKEN is required}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:8001:8001"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 5s

  worker:
    build:
      context: ./apps/worker
      dockerfile: Dockerfile.worker
    container_name: sadar-worker
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy
    environment:
      DATABASE_URL: ${DATABASE_URL:?Set DATABASE_URL to the Supabase pooled connection string}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      LLM_BASE_URL: ${LLM_BASE_URL:-http://host.docker.internal:8080}
      LLM_TIMEOUT: ${LLM_TIMEOUT:-180}
      LLM_MODEL: ${LLM_MODEL:-gemma4-e4b}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      TELEGRAM_CHAT_ID: ${TELEGRAM_CHAT_ID:-}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USER: ${SMTP_USER:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-noreply@sadarbencana.id}
      EWS_PUBLIC_BASE_URL: ${EWS_PUBLIC_BASE_URL:-https://sadarbencana.id}
      EWS_DELIVERY_ENABLED: ${EWS_DELIVERY_ENABLED:-false}
      EWS_LIFECYCLE_DELIVERY_ENABLED: ${EWS_LIFECYCLE_DELIVERY_ENABLED:-false}
      WORKER_API_TOKEN: ${WORKER_API_TOKEN:?WORKER_API_TOKEN is required}
      API_ENV: ${API_ENV:-docker}
      ASSET_POLL_INTERVAL_SECONDS: ${ASSET_POLL_INTERVAL_SECONDS:-60}
      OPENSKY_POLL_INTERVAL_SECONDS: ${OPENSKY_POLL_INTERVAL_SECONDS:-300}
      OPENSKY_BACKOFF_INITIAL_SECONDS: ${OPENSKY_BACKOFF_INITIAL_SECONDS:-900}
      OPENSKY_BACKOFF_MAX_SECONDS: ${OPENSKY_BACKOFF_MAX_SECONDS:-3600}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    ports:
      - "127.0.0.1:8002:8002"

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile.web
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-/api/v1}
        VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:-}
        VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY:-}
    container_name: sadar-web
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    ports:
      - "127.0.0.1:3001:3001"

volumes:
  sadar-redis-data:
