{
  "name": "risk-monitor",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web",
    "dev:api": "cd apps/api && go run ./cmd/server",
    "dev:mastra": "npm run dev --workspace apps/mastra",
    "verify": "bash scripts/verify-structure.sh"
  }
}
